open source
apres .net core 3 la gage est la perf
SDK: Donc en gros SDK sur votre machine de développement et vos machines de test si vous souhaitez.
runtime: Et runtime sur la partie prod.
open api swagger
apratire de .net 6 pas de startup 

http : 
appelle une requête, une requête.

En fait, ça va être une trame HTTP, donc un bloc qui va contenir deux parties, l'en tête et le corps.

Le corps, c'est ce qu'on va envoyer comme demande le contenu qui va permettre d'être interprété par

le serveur.

Et l'entête, c'est toutes les précis	ions de la demande. 